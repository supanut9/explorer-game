name: repo-checks

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - "sprint/**"

jobs:
  repo-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Validate repo structure and planning
        run: python3 scripts/validate_repo.py
